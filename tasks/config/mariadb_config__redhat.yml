---
- name: copy server.cnf to remote node
  ansible.builtin.template:
    src: "{{ mariadb_config.src }}"
    dest: "{{ mariadb_config.path.redhat }}{{ mariadb_config.file.redhat }}"
    owner: root
    group: root
    mode: 0644
    backup: "{{ mariadb_config.backup }}"
  notify: restart mariadb